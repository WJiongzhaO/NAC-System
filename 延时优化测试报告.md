# test_audio 流式平均延时统计

来源：`output/streaming_test_audio/latency_report.json`

| 指标 | 平均值 (ms) | 中位数 (ms) | 标准差 (ms) | 样本数 |
|---|---:|---:|---:|---:|
| ASR 延迟 | 1014.7 | 953.9 | 316.5 | 20 |
| LLM 总耗时 | 2902.7 | 2800.9 | 1041.0 | 20 |
| TTS 延迟 | 6534.4 | 6436.6 | 1222.5 | 20 |
| 首段可播放延迟 | 930.8 | 0.2 | 4161.4 | 20 |
| 端到端总延迟 | 32081.8 | 33357.9 | 5592.6 | 20 |

## 说明

- 统计对象：`data/test_audio` 下全部 20 条音频。
- 流式模式已启用衔接语预热，因此大多数样本的“首段可播放延迟”为 0 ms。
- 个别样本触发安全拦截或问答长度不同，会影响整体平均值。

## subprocess 改 Python API 直调后的结果

来源：`output/streaming_test_audio/latency_report.json`

| 指标 | 平均值 (ms) | 中位数 (ms) | 标准差 (ms) | 样本数 |
|---|---:|---:|---:|---:|
| ASR 延迟 | 992.6 | 990.5 | 253.8 | 20 |
| LLM 总耗时 | 2753.1 | 2715.0 | 963.9 | 20 |
| TTS 延迟 | 1668.7 | 1456.4 | 863.5 | 20 |
| 首段可播放延迟 | 520.7 | 0.2 | 2327.2 | 20 |
| 端到端总延迟 | 16482.4 | 16401.0 | 3671.5 | 20 |

说明：这组结果是在完成 subprocess 改 Python API 直调之后重新评测得到的，TTS 平均延迟相比前一版显著下降。

## 在上一版基础上完成多句 TTS 并发合成后的结果

来源：`output/streaming_test_audio/latency_report.json`

| 指标 | 平均值 (ms) | 中位数 (ms) | 标准差 (ms) | 样本数 |
|---|---:|---:|---:|---:|
| ASR 延迟 | 850.4 | 832.6 | 152.5 | 20 |
| LLM 总耗时 | 3024.3 | 3109.0 | 870.3 | 20 |
| TTS 延迟 | 1395.7 | 1375.1 | 531.1 | 20 |
| 首段可播放延迟 | 424.8 | 0.2 | 1898.5 | 20 |
| 端到端总延迟 | 14778.8 | 15432.9 | 2348.4 | 20 |

说明：这组结果是在上一版 subprocess 改 Python API 直调的基础上，继续完成多句 TTS 并发合成优化后得到的；TTS 延迟和端到端总延迟进一步下降。

## 在上一版基础上完成 ASR 在线 chunk 流式后的结果

来源：`output/streaming_test_audio/latency_report.json`（`run_eval.py --mode streaming`，20 条 `data/test_audio`）

| 指标 | 平均值 (ms) | 中位数 (ms) | 标准差 (ms) | 样本数 |
|---|---:|---:|---:|---:|
| ASR 延迟 | 2605.3 | 2150.8 | 2013.1 | 20 |
| LLM 总耗时 | 2879.0 | 2995.9 | 1365.8 | 20 |
| TTS 延迟 | 850.0 | 725.5 | 435.0 | 20 |
| 首段可播放延迟 | 1385.3 | 790.6 | 2037.1 | 20 |
| 端到端总延迟 | 6655.7 | 6265.7 | 3172.3 | 20 |


### 本版相对「多句 TTS 并发」版的变化

| 指标 | 上一版（TTS 并发） | 本版（+ASR chunk 流式） | 差值 |
|---|---:|---:|---:|
| ASR 延迟 | 850.4 | 2605.3 | **+1754.9 ms** |
| LLM 总耗时 | 3024.3 | 2879.0 | −145.3 ms |
| TTS 延迟 | 1395.7 | 850.0 | **−545.7 ms** |
| 首段可播放 | 424.8 | 1385.3 | +960.5 ms |
| 端到端总延迟 | 14778.8 | 6655.7 | **−8123.1 ms** |

asr延迟明显增加，但端到端总延迟大幅下降，换了个终端运行的原因？


实际在web端运行时和后端run_streaming.py看到的结果还是有较大的差异
例如实现ASR 在线 chunk 流式延时优化前后，web端看到的asr延迟是减少的，而run_streaming.py得到的结果是asr延迟明显增加

且web端看到的总耗时比后端直接运行测试看到的端到端总延迟时长要更长
对此做了Web 复用管线，首次启动时会先进行预热流式管线，把冷启动从「每次请求」挪到「服务启动」，不足之处在于启动更慢、内存常占、不宜高并发。

---

## 总结

### 评测概况

- 对象：`data/test_audio` 共 20 条 wav；命令 `run_eval.py --mode streaming`；数据来自 `latency_report.json`。
- 下文四组表为同一套指标在不同优化阶段的**批量均值**，仅供前后对比参考，**不是精确、可复现的基准值**（机器、LLM API 网络、是否复用管线等都会影响）。

### 各阶段结果与趋势

| 阶段 | 端到端总延迟（均值） | TTS 延迟（均值） | 要点 |
|------|---------------------:|-----------------:|------|
| 优化前（文首表） | 32081.8 ms | 6534.4 ms | TTS 为瓶颈；首段可播放多数接近 0 ms（衔接语已预热） |
| ① TTS 改 API 直调 | 16482.4 ms | 1668.7 ms | 端到端约减半，TTS 明显下降 |
| ② + 多句 TTS 并发 | 14778.8 ms | 1395.7 ms | 端到端、TTS 继续改善 |
| ③ + ASR 在线 chunk 流式 | 6655.7 ms | 850.0 ms | 端到端再降约 8.1 s；**ASR 均值升至 2605 ms** |

**本次相关改动**：

1. TTS：`subprocess` 调 Piper CLI → `PiperVoice` 直调 ONNX。  
2. TTS：多句 `ThreadPoolExecutor` 并发合成。  
3. ASR：`paraformer-zh-streaming` + `chunk_size` / `cache` 在线分块识别。  
4. 评测复用单例管线；Web 启动时单例预热，避免每次请求重复加载 ASR。

### Web / 单条脚本与跑批均值的差异

| 现象 | 原因（简要） |
|------|----------------|
| Web「总耗时」曾明显高于跑批端到端均值（如 ~14s vs ~6.5s） | 改前 Web **每条请求新建** `StreamingPipeline`，ASR 每次冷加载；跑批 **20 条共用一个管线** |
| ASR chunk 前后 Web 与 `run_streaming` 表现不一致 | 单条脚本每次新建管线；Web 与跑批统计字段相同但**运行环境不同** |
| 已做 Web 单例 + 启动预热 | 冷启动挪到服务启动阶段；**预热时间不计入**页面 `total_ms`；启动变慢、内存常驻、不宜高并发 |
