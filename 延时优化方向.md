# 延时优化方向

> 基于当前流式管线（VAD分句 + LLM streaming + TTS逐句 + 衔接语预热）的进一步改进。

## 当前瓶颈

```
VAD → ASR整段 → Filler(0ms) → LLM流式 → TTS逐句(顺序)
       ~700ms       ★           ~1700ms      ~1200ms/句
```

## 六大优化方向

| # | 方向 | 问题 | 方案 | 收益 |
|---|------|------|------|------|
| 1 | subprocess 改 Python API 直调 | TTS 每句 ~1200ms，subprocess 启动开销大 | `piper.PiperVoice` 常驻内存直接 ONNX 推理 | ~1200ms → ~400ms |
| 2 | 多句 TTS 并发合成 | N 句 TTS 串行，N+1 等 N 完成 | `ThreadPoolExecutor` 并发合成 | 总时间 ≈ max(单句) |
| 3 | 单例管线复用缓存 | 批量评测 20 文件 = 60 次 filler 预热 | 静态 filler 缓存 + 评测复用同一管线 | 消除 ~4.5s/条 |
| 4 | ASR 在线 chunk 流式 | 整段音频等完才识别 | `chunk_size=[0,10,5]` + `cache` 逐 chunk 解码 | 首句 0ms（边说边出） |
| 5 | LLM 超时降级容错 | API TTFT 1,278~3,028ms，波动 2.4× | `timeout=3.0` + 衔接语延长兜底 | 避免长尾阻断 |
| 6 | 正则切分改字符串索引 | 每个 token chunk 跑 `re.match` | `str.find()` + 索引切片 | 微优化，句级可忽略 |

