# 延时优化方案

## 总览

| 版本 | 架构 | 首段可播放延迟 | 优化收益 |
|------|------|---------------|----------|
| 基线 | 串行 ASR→LLM→TTS | ~9,000 ms | — |
| 优化 V1 | + 流式 LLM + 逐句 TTS | ~1,000 ms | 88% |
| 优化 V2 | + 衔接语预热 | ~10 ms | 99.9% |

---

## 一、基线架构（优化前）

```
用户语音 → [ASR 整段转写] → [LLM 完整生成] → [TTS 整段合成] → 开始播放
          |—— 512ms ——|——— 2224ms ———|—— 1541ms ——| ← 全程无输出
                              总计 ~9s 用户才听到第一声
```

**问题**：全串行，每个模块等上一个完全结束后才开始，用户长时间沉默等待。

---

## 二、优化 V1：流式级联 + 首句优先播放

### 2.1 架构

```
用户语音 → VAD分句 → LLM(stream) ─首token─→ TTS逐句 → 前端队列播放
                          │                          │
                          └── 后续token ──→ 后续句合成 ──→ 陆续播放
```

### 2.2 改动点

| 模块 | 基线 | 流式改进 |
|------|------|----------|
| ASR | 整段转写，等全文 | VAD分句，~50ms/句，首句即送下游 |
| LLM | `stream=False`，等全文 | `stream=True`，SSE流式，首 token ~700ms |
| TTS | 整段合成，~1500ms | 逐句合成，前端按片段队列连续播放 |

### 2.3 延迟分解

```
VAD尾点检测:  ~200ms
ASR首句转写:   ~50ms
LLM首token:   ~700ms  ← 主瓶颈 (70%)
TTS首包:       ~80ms
─────────────────────
首段可播放:  ~1,030ms
```

### 2.4 关键代码

- `src/pipeline/streaming_pipeline.py` — 流式管线主逻辑
- `src/llm/deepseek_client.py` — `generate_stream()` SSE 流式生成
- `src/asr/paraformer_asr.py` — FSMN-VAD 内置分句
- `web/app.js` — 流式音频播放队列，按 `filler → sent0 → sent1 → ...` 顺序播放完整回答

---

## 三、优化 V2：衔接语预热

### 3.1 核心思路

LLM 首 token 700ms 是硬延迟，无法消除。但可以在等待 LLM 时**播放一段预合成的短衔接语**，用户几乎瞬间听到声音。

```
VAD结束 → [预合成衔接语即刻播放] ──→ [LLM正文首句追上，无缝衔接]
          ↑ 零延迟（文件已就绪）      ↑ LLM在衔接语播放期间已生成完
```

### 3.2 实现

```python
# 预热阶段（管线初始化时）
FILLER_PHRASES = ["好的，", "关于这个问题，", "嗯，请稍等，"]
for phrase in FILLER_PHRASES:
    tts.synthesize(phrase, f"output/filler_{i}.wav")  # 预合成到磁盘

# 运行时
# VAD结束后，直接返回预合成的 filler_{x}.wav 路径作为首段音频
# 后续回答按句合成为 sent0.wav、sent1.wav ...
# 前端把 [filler, sent0, sent1, ...] 作为播放队列连续播放
```

当前实现中，流式模式不是把所有 WAV 片段预先拼接成一个单独文件，而是由前端维护播放队列：先播放预合成衔接语，再依次播放后续回答句子的 TTS 片段。这样可以保留“快速听到第一声”的低延迟优势，同时让用户听到完整回答。

### 3.3 延迟分解

```
VAD尾点检测:  ~200ms
衔接语启动:     ~10ms  ← 预合成文件读取/播放启动
LLM首token:   ~700ms  （衔接语播放期间并行等待）
TTS首包:       ~80ms
─────────────────────
首段可播放:     ~210ms  ← 用户感知：几乎瞬间听到"好的，"
正文首句:    ~1,030ms  ← 衔接语后面自然衔接
```

### 3.4 衔接语设计原则

- **极短**：2~4 字，100~300ms 播放时长，不给用户"废话"感
- **随机**：3 条轮换，避免机械重复
- **自然**：口语化短语（"好的""关于这个""嗯请稍等"）
- **桥接**：衔接语结束时 LLM 正文首句刚好就绪

---

## 四、延迟对比表

| 测量点 | 基线 | V1 流式 | V2 流式+预热 |
|--------|------|---------|-------------|
| ASR 延迟 | 512 ms | 512 ms | 512 ms |
| LLM 首 token (TTFT) | — | 700 ms | 700 ms |
| TTS 首包 | 1,541 ms | 80 ms | 80 ms |
| **首段可播放 ★** | **~9,000 ms** | **~1,030 ms** | **~0~210 ms** |
| 端到端完成 | 9,000 ms | 9,000 ms | 9,000 ms |

> 首段可播放 = 用户停止说话 → 第一次听到声音的间隔（作业核心考核指标）。在 V2 中，第一次听到的通常是预合成衔接语；完整回答仍会在 LLM 生成和 TTS 分句合成后由前端队列继续播放。

---

## 五、未实现但可行的后续优化

| 方向 | 方案 | 预估收益 |
|------|------|----------|
| Piper Python API 真流式 | `PiperVoice` 逐帧回调，替代 subprocess | 首包 ~80ms → ~30ms |
| ASR Chunk流式 | Paraformer chunk-by-chunk 在线识别 | 消除"等整句说完"延迟 |
| 句间无缝拼接 | 后端 PCM buffer 拼接或 Web Audio API 精准调度 | 消除分段播放的轻微句间停顿 |
| 本地 LLM 部署 | GPU 推理替代 API 网络延迟 | LLM TTFT ~700ms → ~200ms |

### 当前局限

当前流式 TTS 采用“分句合成 + 前端队列播放”实现，尚未将多个 WAV 片段在后端拼接为单一音频流，因此句子之间可能存在轻微停顿。后续可通过 PCM buffer 拼接、Web Audio API 队列调度或后端流式音频输出进一步优化连续性。

---

## 六、实现文件索引

| 文件 | 作用 |
|------|------|
| `src/pipeline/baseline_pipeline.py` | 串行基线 |
| `src/pipeline/streaming_pipeline.py` | 流式+衔接语预热（V1+V2） |
| `src/asr/paraformer_asr.py` | ASR 封装（VAD分句） |
| `src/llm/deepseek_client.py` | LLM 客户端（streaming SSE） |
| `src/tts/piper_tts.py` | TTS 封装（逐句合成） |
| `run_web.py` | 本地 Web 演示服务 |
| `web/app.js` | 前端交互与流式音频队列播放 |
| `run_baseline.py` | 基线运行脚本 |
| `run_streaming.py` | 流式运行脚本 |
| `src/eval/run_eval.py` | 批量评测脚本 |
